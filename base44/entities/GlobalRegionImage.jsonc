{
  "name": "GlobalRegionImage",
  "type": "object",
  "properties": {
    "cardId": {
      "type": "string",
      "description": "Unique identifier for the global region card"
    },
    "imageUrl": {
      "type": "string",
      "description": "URL of the image associated with the region"
    }
  },
  "required": [
    "cardId",
    "imageUrl"
  ]
}